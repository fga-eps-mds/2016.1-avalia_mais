<h1>Enquete da empresa</h1>

<h3>Titulo</h3>

<div ng-controller="QuestionController">
  <%= form_for(@question, html: { name: "newQuestionForm" }) do |f| %>
    <div class="field">
      <%= f.text_field :title, class: "large", autocomplete: "off", ng_model: "question" %>
    </div>

    <h3>Adicione as suas opções:</h3>

    <div class="field">
      <%= text_field_tag "options[][title]", @option.title, autocomplete: "off", placeholder: "Option {{$index+1}}", ng_model: "options[$index]", ng_blur: "optionBlur($index, option)" %>
    </div>

    <div class="field">
      <%= text_field_tag "options[][title]", @option.title, autocomplete: "off", placeholder: "Option {{$index+1}}", ng_model: "options[$index]", ng_blur: "optionBlur($index, option)" %>
    </div>

      <div class="field">
      <%= text_field_tag "options[][title]", @option.title, autocomplete: "off", placeholder: "Option {{$index+1}}", ng_model: "options[$index]", ng_blur: "optionBlur($index, option)" %>
    </div>

      <div class="field">
      <%= text_field_tag "options[][title]", @option.title, autocomplete: "off", placeholder: "Option {{$index+1}}", ng_model: "options[$index]", ng_blur: "optionBlur($index, option)" %>
    </div>


    <% if flash[:notice] %>
      <div class="notice"><%= flash[:notice] %></div>
    <% end %>

    <br>

    <div class="actions">
      <%= f.submit "Criar" %>
    </div>

  <% end %>
</div>
