<%= render "navbar/navbar" %>

<div id="body-show-attach">
	
	<h1>Vínculo Solicitado</h1>

	<p><b>Nome da Empresa:</b> </p>
  	<p><b>Endereço:</b> <%= @attach.address %></p>
  	<p><b>CNPJ:</b> <%= @attach.cnpj %></P>
  	<%= image_tag(@attach.photo, :id => "comprovante") %>

  </br></br>

  	<div id="position_buttons">
	  	<%= button_tag(type: 'submit', class: "btn btn-success", id: "btn-vincular") do %> Aprovar Vínculo 
		<% end %>
		<%= button_tag(type: 'submit', class: "btn btn-danger", id: "btn-rejeitar") do %> Rejeitar Vínculo 
		<% end %>
	</div>
</div>