<header id="header-show">
  <%= image_tag("AVALIA_LOGO.png", :alt => "logo") %>
    <form action="#" method="post" class="forme-email-show">
      <div class="input-mail">
          <input type="text" class="input-subscript-show" placeholder="Buscar"  <%= image_tag("tool.svg", :alt => "logo") %>
          <%= image_tag("tool.svg", :width => "22", :height => "22", :id => "button-subscript-show", :onClick => "submit()") %>
        </div>
    </form>
        <nav id="menu_show">
          <ul>
              <li><a href="">Categorias</a></li>&nbsp;&nbsp;&nbsp;
              <li><a href="">Ranking</a></li>&nbsp;&nbsp;
              <li class="pipe">|</li>&nbsp;&nbsp;
              <li><%= link_to "Entrar", login_path %></li>
          </ul>         
        </nav>
</header>

<section id="corpo">
<section id="principal"> 
  <div id="trans">
  <% @segment.each do |s| %>
    <% total = 0%>
    <% @company.each do |f| %>
      <% if s.id == f.segment_id %>
        <% total = total +1 %>
      <%end%>
    <%end%>

    <% if total > 0 %>
      <h2><span><%=s.name%></span><span><%=total%></span></h2>
      <% @company.each do |f| %>
        <% if s.id == f.segment_id %>
          <span> <%=link_to f.name, f%></span>
        <%end%>
      <%end%>
    <%end%>
  <%end%>  
  </div>
  </section> 
<section>

 
