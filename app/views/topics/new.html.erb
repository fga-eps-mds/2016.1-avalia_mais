<%= form_for(@topic) do |f| %>

<%= f.text_field :title, :placeholder => "Título do Tópico" %></br>
<%= f.text_area :body, :placeholder => "Conteúdo Tópico" %></br>

<%= button_tag(type: 'submit', class: "btn btn-primary") do %> Confirmar <% end %><br/>
<% end %>